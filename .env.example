# Webhook que recebe os leads do diagnóstico via POST JSON
# (Google Apps Script, Make, Zapier, n8n ou endpoint do seu CRM).
# Payload: { name, whatsapp, email?, dominio?, nota?, origem, recebidoEm }
# Sem essa variável os leads ficam registrados apenas nos logs do servidor.
LEAD_WEBHOOK_URL=
